# 帧述 运行配置（复制为 .env 后填写真实值；.env 不入库）

# ── M0（当前）──────────────────────────────
DATA_DIR=data
LLM_MODE=manual          # manual=会话模式（文件契约，AI 本人担任 LLM）| api=LLM API（OpenAI 兼容）

# ── LLM（API 模式时启用）：默认 GLM 编码套餐端点 ──
LLM_API_KEY=
# LLM_MODEL=glm-5.3-flash   # 默认；换 glm-5.3 更强
# LLM_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4   # 默认；按量计费/DeepSeek 换 https://api.deepseek.com/v1

# ── TTS（可选，2026-09-23：配音质量升级）─────
# 硅基流动 CosyVoice2（有免费额度，中文比 Kokoro 自然得多）；
# 注册 https://siliconflow.cn → API Keys → 填到这里即自动启用，无 key 回落 Kokoro 离线
SILICONFLOW_API_KEY=
# TTS_VOICE=FunAudioLLM/CosyVoice2-0.5B:alex   # 备选 :benjamin :charles :david，出片 A/B 后定

# ── PG（案卷库）────────────────────────────
# 配了但连不上 = 服务拒绝启动（fail-fast，防静默丢聊天记录）；开发机临时没库：
# 注释掉 PG_DSN（内存模式）或设 STORE_FALLBACK=1 显式接受降级
PG_DSN=host=127.0.0.1 port=5432 user=zhenshu password=TODO dbname=zhenshu sslmode=disable

# ── 运维可选 ───────────────────────────────
# FFMPEG_BIN=C:\path\to\ffmpeg\bin   # PATH 里没有 ffmpeg 时指定（目录或 exe 完整路径）
# ADDR=127.0.0.1:8080                # 监听地址，默认仅本机；服务器/容器部署才需要 :8080

# ── AI 生图（2026-09-26：image 元素 source=gen / 搜图失败兜底）──
# 智谱 CogView，与 LLM 同 key 体系（实测套餐 key 可调免费档）；IMG_GEN_KEY 缺省用 LLM_API_KEY
IMG_GEN=1
IMG_GEN_MODEL=cogview-3-flash   # 免费档，右下角带"AI生成"水印；cogview-4 无水印但需按量余额（1113=余额不足）
# IMG_GEN_KEY=
