# 帧述 运行配置（复制为 .env 后填写真实值；.env 不入库）

# ── M0（当前）──────────────────────────────
DATA_DIR=data
LLM_MODE=manual          # manual=会话模式（文件契约，AI 本人担任 LLM）| api=DeepSeek

# ── LLM（API 模式时启用）────────────────────
DEEPSEEK_API_KEY=sk-xxx

# ── TTS（可选，2026-09-23：配音质量升级）─────
# 硅基流动 CosyVoice2（有免费额度，中文比 Kokoro 自然得多）；
# 注册 https://siliconflow.cn → API Keys → 填到这里即自动启用，无 key 回落 Kokoro 离线
SILICONFLOW_API_KEY=
# TTS_VOICE=FunAudioLLM/CosyVoice2-0.5B:alex   # 备选 :benjamin :charles :david，出片 A/B 后定

# ── M1：原服务器（47.94.247.12）─────────────
# 2026-09-22 已通：端口映射 + 安全组放行完成，库 zhenshu / bucket zhenshu 已建
PG_DSN=host=47.94.247.12 port=5432 user=zhenshu password=TODO dbname=zhenshu sslmode=disable
MINIO_ENDPOINT=47.94.247.12:9000
MINIO_ACCESS_KEY=TODO
MINIO_SECRET_KEY=TODO
MINIO_BUCKET=zhenshu

# Redis 暂不接入（tech-stack §2），值留档
REDIS_ADDR=47.94.247.12:6379
